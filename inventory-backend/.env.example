# ==========================================
# Orbis Backend - Environment Variables
# ==========================================
# Copy this file to .env and update with your actual values

# ==========================================
# DATABASE CONFIGURATION
# ==========================================
# PostgreSQL connection string used by Prisma
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# 
# Neon database example:
# DATABASE_URL="postgresql://user:password@ep-cool-name-123456.us-east-2.aws.neon.tech/neondb?sslmode=require"
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME?schema=public"

# ==========================================
# JWT CONFIGURATION
# ==========================================
# Secret key for signing JWT tokens (change in production!)
JWT_SECRET="your-super-secret-key-change-this-in-production"

# ==========================================
# EMAIL VALIDATION - ABSTRACT API
# ==========================================
# Get your FREE API key from: https://www.abstractapi.com/api/email-validation
# Free Tier: 100 validations/month (sufficient for testing)
# Upgrade when: You exceed 100 validations/month
# 
# This API validates email format, deliverability, and detects disposable addresses
ABSTRACT_API_KEY="your-abstract-api-key-here"

# ==========================================
# EMAIL SENDING - BREVO API
# ==========================================
# Get your FREE API key from: https://www.brevo.com
# Free Tier: 300 emails/day (≈9,000/month - sufficient for current use)
# Upgrade when: You exceed daily email quota
# 
# Brevo (formerly Sendinblue) is used to send transactional emails
BREVO_API_KEY="your-brevo-api-key-here"

# Email from name and address
EMAIL_FROM="noreply@inventorymanager.com"
EMAIL_FROM_NAME="Inventory Manager"

# Support email for user inquiries
SUPPORT_EMAIL="support@inventorymanager.com"

# Frontend application URL (for verification links sent in email)
# For React Native Expo development: exp://localhost:19000
# For React Native with custom scheme: orbis://verify-email
# For web app: https://app.inventorymanager.com
APP_URL="orbis://verify-email"

# ==========================================
# LEGACY EMAIL CONFIGURATION (OPTIONAL - NOT USED FOR NEW EMAIL VERIFICATION)
# ==========================================
# NOTE: These settings are ONLY used for:
# - Password reset OTP emails (old OTP flow)
# - Any other future transactional emails
#
# NEW EMAIL VERIFICATION uses: Abstract API + Brevo ONLY (see above)
# These legacy settings can be left as defaults unless you need them
#
# EMAIL_PROVIDER: test (logs to console), smtp, or sendgrid
EMAIL_PROVIDER="test"

# SMTP Configuration (optional, only if EMAIL_PROVIDER=smtp)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-app-password"

# SendGrid Configuration (optional, only if EMAIL_PROVIDER=sendgrid)
# SENDGRID_API_KEY="SG.xxxxxxxxxxxxxxxxxxxxxxxxx"

# ==========================================
# SERVER CONFIGURATION
# ==========================================
NODE_ENV="development"
PORT=3000

# ==========================================
# CORS CONFIGURATION (OPTIONAL)
# ==========================================
# Leave empty in development to allow all origins
ALLOWED_ORIGINS=""